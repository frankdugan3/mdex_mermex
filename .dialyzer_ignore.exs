[
  # `Lumis` is an optional syntax-highlighting engine for `:mdex`. It is not a
  # dependency of this package, so `:mdex`'s own type spec referencing
  # `t:Lumis.options/0` is an unknown type from our perspective. This warning
  # originates entirely in dependency code and is not actionable here.
  ~r/Unknown type: Lumis\.options\/0/
]
